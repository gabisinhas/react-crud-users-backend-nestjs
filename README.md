# react_crud_users

This a CRUD simple application allows users to be registered and there is also a view to list all users registered.

It was built in front end using React, and Mui Design System from Google and Back End using Nestjs and MongoDB.
